name: serv_oeste
description: "Front-end do servOeste"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  masked_text: ^1.1.2
  logger: ^2.2.0
  drop_down_search_field: ^1.0.4
  persistent_bottom_nav_bar: ^6.2.1
  super_sliver_list: ^0.4.1
  animated_custom_dropdown: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - images/servOeste.png
